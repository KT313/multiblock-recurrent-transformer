# (c) 2025-2026 Tobias Kerner. Apache-2.0.
"""Terminal user interface: the rich dashboard with live progress bars and a log section (`dashboard.py`) and the
console capture both live dashboards — this one and `training/ui/` — install around their display (`capture.py`)."""
